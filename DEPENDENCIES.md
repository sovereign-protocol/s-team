# Dependency inventory

Runtime: `sovereign-protocol>=0.1.9,<0.2` (`LGPL-3.0-or-later`), installed as a
separately replaceable Python library. Optional: pywebview `>=5,<6` (BSD-3-Clause),
for the desktop window only. Development only: pytest `>=8,<10` (MIT).
Sovereign Core's transitive runtime inventory is published in its repository.
No dependency is bundled in the S-Team source distribution or wheel.
